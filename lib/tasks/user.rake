namespace :db do

  desc "Finding user infromation"

  task :user => :environment do

    # require "i18n"
    # message = "Vieux-montreal"

    # contain = I18n.transliterate(message)
    # contain.gsub!(/[^0-9A-Za-z]/, ' ')
    # contain.downcase!
    # p contain.split

    hours = ['heure', 'hour', 'hours', 'ouverture', 'opening']



  end

end
